import React from 'react';
import { storiesOf } from '@storybook/react';
import toast from './src/index.js';

storiesOf('Toast', module)
  .add('default', () => (<button onClick={() => {
        toast({
            text: 'hello'
        });
    }}>toast</button>))