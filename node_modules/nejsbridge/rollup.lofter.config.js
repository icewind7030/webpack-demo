import babel from 'rollup-plugin-babel';
import resolve from 'rollup-plugin-node-resolve';
import commonjs from 'rollup-plugin-commonjs';

export default {
	input: 'src/lofter/lofter.js',
	name: 'Bridge.lofter',
    plugins: [
        commonjs({

        }),
        resolve({
            main: true
        }),
      babel({
        exclude: 'node_modules/**',
        externalHelpers: false,
        externalHelpersWhitelist: ['inherits','classCallCheck','possibleConstructorReturn','createClass','typeof']
      })
    ],
	output: [
		{ file: 'dist/bridge.lofter.es.js', format: 'es' },
    { file: 'dist/bridge.lofter.iife.js', format: 'iife' },
    { file: 'dist/bridge.lofter.umd.js', format: 'umd' }
    ]
};